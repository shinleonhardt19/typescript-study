// Basic tuples:

let point:[number, number] = [10,20] // x and y coordinates

//console.log(`X = ${point[0]}, Y = ${point[1]}`); // we use this `` to display to the screen whenever using fetch variable

// Tuple with different types of arrays

let person: [string, number] = [`Bronya`,19]; // name, signature_number


function my_waifu(name: string, signature_number: number)
{
    console.log(`Name: ${name}, Signature number: ${signature_number}`)
}


my_waifu(`Bronya`, 19)


